# URL of the deployed Google Apps Script Web App (ends in /exec).
# See scripts/apps-script-rsvp.gs for the script to deploy.
# Must be NEXT_PUBLIC_ prefixed: the RSVP form calls this URL directly
# from the browser (no server-side proxy) to avoid an extra network hop.
NEXT_PUBLIC_RSVP_SHEET_URL=
